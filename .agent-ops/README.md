# Local-Roots agent-ops adapter

This folder configures `agent-ops` for this repo.

- `config.toml`: policy (autonomy level, budgets, allowlists)
- `commands.toml`: canonical build/lint/test commands
- `prompts/`: per-agent prompts tuned to this codebase
