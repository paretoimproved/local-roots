It's gitignored — so it won't be committed or shared with teammates. That's fine for personal permissions, but if you want it shared across the team you'd need to add an exception to `.gitignore` (e.g., `!.claude/settings.json`). Up to you whether that's worth it.