ADDR=:8080
ENV=dev

# Next up (phase 1):
DATABASE_URL=postgres://localroots:localroots@localhost:5432/localroots?sslmode=disable
# JWT secret used to sign seller/buyer tokens (set a long random value in production).
JWT_SECRET=dev-only-change-me
# Optional: comma-separated explicit origins to allow for CORS (Vercel *.vercel.app is allowed by default).
# CORS_ALLOW_ORIGINS=http://localhost:3000,https://local-roots-ideaforge-ddad2823.vercel.app
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
